run:
  timeout: 5m
linters:
  enable:
    - govet
    - staticcheck
    - golint
    - gocyclo
    - gosec
    - ineffassign
  disable:
    - errcheck
    - revive
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
